# Environment Configuration

# Node environment (development/production)
# In development mode, future-dated posts are visible for preview
# In production mode, future-dated posts are hidden until their publish date
NODE_ENV=development

# To hide future posts even in development, set this to false
# SHOW_FUTURE_POSTS=false
